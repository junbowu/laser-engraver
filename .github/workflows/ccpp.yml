name: CMake build test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    
    steps:
    - name: Install Qt
      uses: jurplel/install-qt-action@v1
    - name: cmake
      run: cmake .
    - name: make
      run: make
    - name: make check
      run: make check
